import { FitnessShell } from '@/components/layout/FitnessShell'

export default function FitnessLayout({ children }: { children: React.ReactNode }) {
  return <FitnessShell>{children}</FitnessShell>
}
